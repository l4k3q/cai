# CAI//OPS 开发流程总览

> 本文档梳理整个项目从零到交付的完整开发流程，供快速理解项目全貌。
> 依据：`DEVELOPMENT.md`（08-04）、`HANDOFF.md`（08-15）、`question-library-building-requirements.md`、`question-learning-development.md`、`cai-ops/` 交付包、`webui/` 源码。
> 生成日期：2026-09-07

---

## 0. 一句话理解这个项目

**CAI//OPS = 开源安全 AI 框架 CAI 的二次开发，最终做成一个"CTF 题库学习平台"**：

- 底层：CAI 框架（Agent + 安全工具 + LiteLLM 多模型网关）
- 中层：自研 Web 操作台（对话 + 实时 trace + 每会话自定义模型）
- 上层：题库学习系统（CAI 真实做题 → 验证 → 总结成教学方法 → 用户学习时直接讲解）

---

## 1. 时间线总览

```
阶段1 (~08-04)         阶段2 (~08-15)         阶段3 (设计期)         阶段4 (实现期)
环境搭建 + Web操作台 → 运行时排障加固     →   题库需求 → 技术规格 →  题库功能编码交付
DEVELOPMENT.md         HANDOFF.md             requirements.md        cai-ops/ + infra/
                                              development.md (规格)  demo.sh
```

**文档演进关系**（谁产出谁、谁指导谁）：

| 文档 | 角色 | 对应阶段 |
|---|---|---|
| `DEVELOPMENT.md` | Web 操作台的完整开发记录 + 踩坑日志 | 阶段 1 |
| `HANDOFF.md` | 排障交接文档（给下一个接手的 agent/开发） | 阶段 2 |
| `question-library-building-requirements.md` | 题库构建**需求**（FR-01~FR-12，只说做什么不说怎么做） | 阶段 3a |
| `question-learning-development.md` | 题库学习**技术规格**（919 行，As-Is/To-Be/契约/验收，唯一交付设计文档） | 阶段 3b |
| `cai-ops/delivery-readme.md` | 实现交付包的部署说明 | 阶段 4 |

---

## 2. 阶段 1：环境搭建 + Web 操作台（DEVELOPMENT.md）

### 2.1 做什么

在开源框架 CAI（Cybersecurity AI，Agent 架构 + 按 kill-chain 分组的安全工具 + 300+ 模型支持）之上：

1. 搭建完整开发环境
2. 开发一个 Web 前端操作台（`webui/`），集成进 CAI 自带 HTTP API

### 2.2 关键决策及理由

| 决策 | 理由 |
|---|---|
| Windows + WSL2 (Ubuntu) | CAI 官方推荐 Windows 用户走 WSL |
| Python 3.12.13 + uv | 官方推荐版本；uv 免 sudo、可编辑安装（改代码即时生效） |
| 前端零构建 vanilla HTML/CSS/JS | 安全工具场景：无 npm 供应链风险、易改易扩展；后端 FastAPI 直接挂载静态目录，同源零配置 |
| Neubrutalism 设计风格 | 用户指定（3px 黑边框、硬阴影 `8px 8px 0 #000`、0 圆角、纯黑/橙/米白配色、不对称三栏） |
| `CAI_LICENSE_OFF=1` | 开源模式，跳过 Alias license 检查 |

### 2.3 实现的功能

- **三栏布局**：左侧会话管理 / 中间对话 / 右侧实时 trace
- **SSE 流式对话**：`reasoning_step`（tool_call / tool_output / handoff / agent_switched）逐条渲染
- **每会话自定义 provider（本阶段重点改造）**：
  创建会话流程 = 选 agent → 填 BASE URL + API KEY → FETCH MODELS 拉取模型列表 → 自动补 provider 前缀 → 建会话

### 2.4 后端改动清单（6 个文件）

| 文件 | 改动 |
|---|---|
| `api/app.py` | ① `/` 静态挂载 webui（`CAI_WEBUI=0` 可关）；② 新增 `POST /api/v1/models/fetch`；③ `create_session` 透传 `base_url/api_key` |
| `api/schemas.py` | 请求模型加 `base_url`/`api_key` 字段 |
| `api/sessions.py` | `SessionState` 存 provider 凭据 |
| `api/streaming.py` | SSE 异常时发 `error` 事件（原来流崩溃导致前端永远 loading） |
| `util/config_utils.py` | 新增 `apply_provider_to_agent`：递归给 agent 树（含 handoffs）的 model 对象挂 `_provider_base/_provider_key` |
| `sdk/agents/models/openai_chatcompletions.py` | `_fetch_response` 注入 `api_base/api_key` 进 litellm kwargs；deepseek 分支 `pop("stream_options")` |

**凭据传递链路**（理解这个就理解了整个 provider 机制）：

```
POST /sessions (base_url/api_key)
  → SessionState 保存
  → apply_provider_to_agent 给 model 对象挂 override
  → Runner.run → _fetch_response 读取 override
  → 注入 litellm kwargs → 覆盖 .env 全局配置
```

### 2.5 本阶段的排障精华：DeepSeek 无限 loading（四层叠加根因）

**症状**：用 DeepSeek 建会话发消息，前端永远 loading，随后服务器卡死。

| 层 | 问题 | 修复 |
|---|---|---|
| 前端 | 模型名没补 `deepseek/` 前缀，litellm 报 Provider NOT provided | `normalizeModelId` 按名称模式自动补前缀 |
| 后端 API | **端点忘了透传 base_url/api_key**（schema 加了字段但端点没传，override 从未生效）| 端点补透传（核心修复） |
| 模型层 | DeepSeek 严格拒绝非流式下的 `stream_options` 参数 | deepseek 分支 pop 掉该参数 |
| SSE 层 | 异常时流崩溃，无 `error` 事件 → 前端无限等待 | 异常时发 `error` 事件 + 前端兜底检测 |

**定位方法论**（值得记住）：
1. litellm 单独调用成功 → 排除 DeepSeek 服务问题
2. 脚本 `Runner.run` + .env 成功 → 排除 CAI 模型层
3. 在 `_fetch_response` 加临时调试写文件 → 发现 `_provider_base=MISSING`
4. 对比 `create_session` 端点代码 → 找到漏传

其他修复：mnapi 长跑偶发超时（litellm 连接池脏连接，重启可解）；对话框不能滚动（grid 子项缺 `min-height: 0`）；agent 显示名 vs 内部 id 不一致；fetch models 留空回退 .env。

---

## 3. 阶段 2：运行时排障加固（HANDOFF.md）

### 3.1 背景变化

- 模型网关默认使用 DeepSeek 官方地址 **https://api.deepseek.com**（不追加 `/v1`），默认模型为 `deepseek/deepseek-v4-flash`
- 项目从"开发功能"进入"实际使用 + 稳定性"阶段

### 3.2 修复 1：工具执行 2000 秒卡死

**问题**：同步工具执行路径（`tools/executor.py`）的 `readline()` 对"无输出命令"（如 Pollard's rho 分解 RSA 模数，静默跑很久）永久阻塞。

**修复**：
- `subprocess.Popen(..., start_new_session=True)` 独立进程组
- 后台线程读输出，主循环轮询 `time.time() - start >= timeout`
- 超时 `os.killpg(SIGKILL)` 杀整个进程组，追加超时标记

### 3.3 修复 2：前端实时可见性

模型思考时用户看不到任何动静 → 新增 `toolActivityLabel`：气泡显示「▊ 正在调用 execute_code · <命令前 70 字符>」，status/tool_call/tool_output 事件实时更新。

### 3.4 功能：对话框上传附件

`POST /api/v1/chat-attachments`——只做**读取 + 参与题库指纹匹配**，不自动入库。为阶段 3 的题库定位埋下伏笔。

### 3.5 重大诊断：「十多分钟无响应」（教科书级排障）

**三层叠加根因**：

1. **直接根因**：上游网关对每个请求返回 503 `SERVICE_BUSY`（curl 最小请求复现）
2. **表现为卡死**：litellm 内部重试 3 次 + CAI 层继续重试下一轮 → **静默空转循环，错误不回传前端**
3. **次要隐患**：用 **py-spy 抓主线程栈**，发现卡在 litellm 的 `traceback.format_exc()` → Python 3.12 的 `co_positions()` 慢路径，可能拖死事件循环

**诊断手段**：curl 直接复现上游故障 / py-spy 对运行中进程抓栈（`py-spy dump --pid <PID>`）/ 分析 `~/.cai/logs/*.jsonl` 结构化日志。

### 3.6 留下的待办（当前开发入口）

| 优先级 | 待办 | 思路 |
|---|---|---|
| 高 | 503 网关错误快速失败 + 前端可见 | litellm_adapter 的 except 识别 ServiceUnavailableError → SSE 发 error → 前端显示「⚠ 网关繁忙」 |
| 高 | 限制重试次数 + 模型调用硬超时 | `num_retries=0` 收敛到 CAI 层；总重试 2~3 次即抛前端 |
| 中 | traceback 格式化加 limit | 启动时 monkeypatch `format_exc(limit=50)` 规避慢路径 |
| 低 | 流式输出 reasoning_content；清理 /tmp 残留 | — |

---

## 4. 阶段 3：题库学习功能设计（需求 → 规格）

### 4.1 为什么需要题库（业务动机）

当前 CAI 每次都"重新做题"：用户问一道做过的题，agent 从零跑一遍，慢、贵、不可复用。要变成：**做过的题直接讲解，没做过的题才现场解**。

### 4.2 需求文档（3a）：定义"做什么"

核心思想（FR-01 ~ FR-12）：

```
题目 + 全部参考资料
  → CAI 独立复现完整解题（不是改写 writeup！）
  → 保留真实过程（动作/输出/结果/未完成原因）
  → 复现成功后才总结：整体思路 + 大步骤（目标/原因/原理/动作/结果）
  → 存为后续 CAI 可读取的题库内容
```

**红线**：
- 不能跳过真实复现，直接把参考资料改写成讲解
- 未完成复现时，不得生成看似完整的"标准解法"
- 大步骤的动作和结果必须能追溯到真实复现记录，不得编造

### 4.3 技术规格（3b）：定义"怎么做"

919 行规格文档是唯一开发交付文档，方法论上采用 Diátaxis / arc42 / C4。核心内容：

**① As-Is 分析**——现有系统只是 Agent 操作台（Session → Runner → SSE），逐一列出 9 项缺口（没有 Question 实体、没有题目/方法分离、没有持久化……）。

**② To-Be 双路径架构**：

```
【学习路径】（用户侧）
首条题面 → QuestionResolver（指纹/全文召回 + LLM 只确认候选身份）
  → 命中 → 绑定 Session → 默认讲解（explain 模式，不重新做题）
  → 命中但方法不可用 → live_solve 现场解
  → 未命中 → 降级为原有自由对话（保证不回归）

【建设路径】（管理侧）
Admin 提交题目+资料+成功判据 → PG + MinIO → Outbox → Redis
  → Worker → CAI Runner 真实复现（RunRecord + Trace 存档）
  → 关卡一：终态是否满足 success_criteria（flag/checker/输出标记/状态断言）
  → Synthesizer：Trace → SolutionMethod + 大步骤
  → 关卡二：每个步骤的证据能否回溯到真实 Trace 事件
  → 两关全过 → usable → 原子切换当前方法指针
```

**③ 关键领域模型**：

| 实体 | 职责 | 状态机 |
|---|---|---|
| `Question` | 题目身份（指纹、判据），**不含答案** | draft → building → usable / incomplete |
| `SolutionMethod` | 解题方法版本（思路 + 步骤 + 证据引用） | draft → verifying → usable → superseded / failed |
| `RunRecord` | 单次真实运行的事实 | queued → running → verified / incomplete / failed |
| `BuildJob` | 后台构建任务进度 | queued → running → verifying_result → synthesizing → verifying_method → usable |

**④ 铁律**：
- 题目与方法**严格分离**——防止题目身份被解法污染（LLM 确认候选时只能看题面/资料身份证据，不能看 flag、漏洞分类、命令路线）
- 4 套状态机不得混用
- `evidence_refs` 只能引用 verified run 下真实存在的事件 ID
- flag 真实值只在 Verifier 内可读，不进 API/日志/模型上下文

**⑤ 定量验收门槛**：
- 同题格式变体召回率 ≥ 95%
- 相似题误绑定率 = 0（不确定就返回 insufficient，宁可不绑）
- Resolver P95 延迟 < 2 秒

---

## 5. 阶段 4：题库功能实现与交付（cai-ops/ + infra/）

### 5.1 交付物结构

```
cai-ops/
├── question_bank/                  # 核心包
│   ├── models.py                   # SQLAlchemy 2.0 async ORM — 9 张 PG 表
│   ├── schemas.py                  # Pydantic 输入输出模型
│   ├── repository.py               # 异步 PostgreSQL CRUD
│   ├── materials.py                # MinIO/S3 资料存储（≤100 MiB/文件）
│   ├── fingerprints.py             # 题面规范化 + SHA-256 实例指纹
│   ├── resolver.py                 # 题目定位（候选召回 + LLM 确认）
│   ├── builder.py                  # BuildOrchestrator（CAI→Gate1→合成→Gate2）
│   └── routes.py                   # 6 个 API 端点
├── question_bank_worker/main.py    # Redis Worker（Outbox Dispatcher + Build Worker）
├── migrations/                     # DDL：001 建表 + 002 runtime_config
├── patch_app*.py                   # 补丁脚本：把 Resolver 集成进 send_message_stream
└── test_resolver*.py, test_sse.sh  # 测试脚本

infra/docker-compose.yml            # PostgreSQL 16 + Redis 7 + MinIO（全带 healthcheck）
demo.sh                             # 端到端演示（9 项检查）
```

### 5.2 实现要点

- **指纹幂等去重**：同一题面重复提交 → SHA-256 指纹相同 → 返回已有 Question，不重复创建
- **Outbox 模式**：API 在数据库事务中写任务 + outbox 事件，独立投递器再发 Redis——避免"数据库已创建但队列未收到"的丢任务窗口
- **Worker 幂等领取**：任务 ID 去重，服务重启后可从数据库恢复未完成任务
- **教学上下文注入**：命中题库后，把 SolutionMethod 格式化为教学 prompt 注入 agent（"先讲整体思路，再逐步讲解，用户可要求复现"）
- **路由注册陷阱**（delivery-readme 特别标注）：新路由必须放在旧 `GET /question-bank/{bank_id}` 之前，否则 `/questions` 会被 `{bank_id}` 参数误匹配

### 5.3 部署流程（8 步）

```
1. docker compose up -d（起 PG/Redis/MinIO）
2. 创建 MinIO bucket
3. 执行 DDL 建表
4. uv add sqlalchemy asyncpg
5. 复制代码到 CAI 项目 src/cai/
6. app.py 注册路由（注意顺序）
7. 重启 CAI 服务
8. 启动 Worker（python -m cai.question_bank_worker.main）
```

### 5.4 验证

`demo.sh` 端到端跑通：创建 CTF 题（Win32k 提权/Redis 未授权/Log4Shell）→ 指纹去重 → 分页/详情查询 → 解法查询（409 尚无可用解法）→ 直连 PG 查表验证持久化 → Redis 队列状态 → MinIO 对象检查。

---

## 6. 最终架构全景

```
┌─────────────────────────────────────────────────────────────────┐
│ Browser (webui/ — Neubrutalism vanilla JS)                      │
│   会话管理 │ 对话+SSE │ 实时trace │ Q BANK构建 │ 附件上传        │
└──────────────┬──────────────────────────────────────────────────┘
               │ HTTP JSON + SSE
┌──────────────▼──────────────────────────────────────────────────┐
│ FastAPI (src/cai/api/)                                          │
│   Session API │ models/fetch │ chat-attachments                 │
│   Question Bank API (v2, PG-backed) │ Resolver 集成点            │
└───────┬────────────────┬───────────────────┬───────────────────┘
        │                │                   │
        ▼                ▼                   ▼
┌─────────────┐  ┌──────────────┐  ┌─────────────────────────────┐
│ CAI Runner  │  │ PostgreSQL16 │  │ Redis 7                     │
│ (Agent/Tool │  │ 元数据：     │  │ build_jobs:queue            │
│  /MCP/Handoff)│ │ 9张表+outbox │  └──────────┬──────────────────┘
│  ↓ litellm  │  └──────┬───────┘             │
│  ↓ 模型网关  │         │                     ▼
│ (tokenrhythm│         │            ┌─────────────────────────────┐
│  .studio)   │         └───────────►│ Builder Worker              │
└─────────────┘                      │  Outbox Dispatcher          │
        │                            │  BuildOrchestrator:         │
        ▼                            │   CAI复现→Gate1→合成→Gate2   │
  MinIO (S3) ◄───────────────────────┤  Trace/资料/日志 存档       │
  资料/Trace/构建日志                  └─────────────────────────────┘
```

---

## 6.5 阶段 5：旁观 Agent（SDD + TDD + spec-kit，2026-09-09）

本阶段引入 **GitHub spec-kit 规格驱动开发工作流**，为 CAI 添加"旁观 agent"：
自动观察对话 → 每满 N 轮评估意图 → 求知则推荐题库题目 → 确认后复用既有
explain 路径教学。全程 SDD（先规格后代码）+ TDD（先失败测试后实现）。

**spec-kit 流程产物**（`specs/001-spectator-agent/` + `.specify/`）：

| 阶段 | 产物 | 要点 |
|---|---|---|
| `/constitution` | `.specify/memory/constitution.md` v1.0.0 | 7 条宪法：零回归/快速失败/测试先行/显式动作/题解分离/旁观不干扰/简单复用——全部源自项目既有红线 |
| `/specify` | `spec.md` | 5 个用户故事（P1 静默评估→P5 零回归）、15 条 FR、7 条 SC |
| `/clarify` | spec.md Clarifications 节 | 4 问 4 答：每 N 轮边界评估/仅 JSONL 日志/绑定后会话级停用/卡片无自动过期 |
| `/plan` | plan/research/data-model/contracts/quickstart | 8 项 ADR 决策（钩子位置/轮询通道/复用 explain/内存评分等） |
| `/tasks` | tasks.md | 28 个任务按用户故事分组，全部标注测试先行 |
| `/implement` | `cai-ops/spectator/`（核心包 97 测试全绿）| 核心包零 CAI 依赖（Windows 可测）+ `spectator_routes.py`（3 端点）+ `patch_spectator_app.py`（幂等补丁）+ webui 三件套（旁观面板/轮询/卡片） |

**关键设计决策**（详见 specs/001-spectator-agent/research.md）：
- 评估钩子在消息流完成后异步触发（不阻塞主链路）；卡片经独立轮询端点下发
  （SSE 协议零改动）；accept 端点只做会话绑定，教学 100% 复用 patch_v2 的
  explain 注入路径；匹配用确定性关键词评分（题面 1.0/解法原理 0.6 权重 +
  冷却惩罚），不引入向量检索。

**待 WSL 侧验证**（T013/T027，见 specs/001-spectator-agent/quickstart.md）：
应用补丁 → 开启 `CAI_SPECTATOR_ENABLED=true` → 3 轮求知对话 → 卡片出现 →
接受 → 讲解接管；既有回归测试双状态各跑一遍。

---

## 7. 工程方法论总结（本项目最有价值的部分）

| 方法 | 在项目中的体现 |
|---|---|
| **文档驱动开发** | 需求(FR) → 技术规格(As-Is/To-Be/契约/验收) → 实现 → 交付文档；规格要求"待权衡项必须写成 ADR，不得静默选择" |
| **分层排障法** | 最小复现（litellm 直测）→ 隔离验证（Runner.run）→ 注入调试（写文件看变量）→ 代码比对；每一步排除一层 |
| **观测工具武装** | py-spy 抓栈定位事件循环阻塞；curl 复现上游故障；headless Chrome CDP 验证 UI 滚动；jsonl 结构化日志 grep |
| **快速失败原则** | 把"静默重试十几分钟"作为核心教训——后续所有设计都强调错误必须立即、可见地回传 |
| **防呆纵深设计** | 两关卡验证防"假解法"；题目/解法分离防身份污染；Outbox 防丢任务；幂等 key 防重复提交；灰度开关+降级路径防回归 |
| **双环境协作** | Windows（编辑源）↔ WSL（运行时），`MSYS_NO_PATHCONV=1 wsl -e bash -lc '...'` 统一运维入口 |
| **兼容性红线** | 题库功能关闭/依赖故障时，现有自由对话必须 100% 不回归（写进验收测试） |

---

## 8. 当前状态与下一步

**已完成**：
- ✅ Web 操作台（对话/trace/自定义 provider/附件）
- ✅ 运行时关键修复（executor 超时、SSE error 事件、前端实时可见性）
- ✅ 题库持久化层 + 管理 API + Worker（通过 demo 验证）
- ✅ Resolver 集成补丁（patch_app*.py，需按 delivery-readme 应用）
- ✅ 旁观 agent（spec-kit SDD/TDD，核心包 97 测试全绿；WSL 集成验证待执行，见 specs/001-spectator-agent/quickstart.md）

**待完成**（按 HANDOFF.md 优先级）：
1. 🔴 旁观 agent WSL 侧集成验证（quickstart ②③ 级 + 既有回归双状态）
2. 🔴 503 网关错误快速失败 + 前端可见（litellm_adapter except → SSE error → 前端提示）
3. 🔴 重试上限 + 模型调用硬超时（`num_retries=0`，重试收敛到 CAI 层）
4. 🟡 traceback.format_exc 加 limit（monkeypatch 规避 Python 3.12 慢路径）
5. 🟢 流式输出 reasoning_content；清理 /tmp 残留文件
6. 🟢 题库学习路径的完整前端（步骤展示、显式复现按钮——规格 §7.5 已定义边界）

**运维速查**（从 Windows 操作 WSL）：

```bash
MSYS_NO_PATHCONV=1 wsl -e bash -lc 'curl -s -m 8 http://localhost:8000/api/v1/health'   # 健康检查
MSYS_NO_PATHCONV=1 wsl -e bash -lc 'pkill -9 -f "cai --api --api-port 8000"'            # 停后端
MSYS_NO_PATHCONV=1 wsl -e bash -lc 'cd /home/l4k3q/cai && setsid nohup uv run cai --api --api-port 8000 >/tmp/cai_api.log 2>&1 </dev/null &'  # 起后端
MSYS_NO_PATHCONV=1 wsl -e bash -lc 'tail -n 50 /tmp/cai_api.log'                        # 看日志
```

---

## 9. 新人快速上手路径（建议阅读顺序）

1. 本文 —— 建立全貌
2. `DEVELOPMENT.md` §3-§6 —— 理解前端架构和 provider 链路、经典排障案例
3. `HANDOFF.md` §5-§6 —— 理解模型调用链、SSE 架构和当前待办
4. `question-learning-development.md` §2→§4→§6 —— 理解 As-Is/To-Be 差距和两条运行时流程
5. `cai-ops/delivery-readme.md` —— 理解交付物部署
6. 动手：跑 `demo.sh`，再从 `question_bank/routes.py` → `builder.py` → `worker/main.py` 读实现
